version: '3.8'

services:
  biosearch-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: biosearch-backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 1.5G
